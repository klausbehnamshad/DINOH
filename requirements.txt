# pip fallback for the C²DH Oral History evaluation (conda users: see environment.yml).
#
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# Pins kept to compatible ranges so "restart kernel & run all" is reproducible.
pandas>=2.0,<3.0
jsonschema>=4.18,<5.0
referencing  # explizit sichtbar (wird transitiv von jsonschema>=4.18 gezogen; hier gepinnt für CI-Reproduzierbarkeit)
jinja2>=3.1.2  # pandas 2.x Styler.to_latex braucht das; vorher nur transitiv via jupyterlab (C1-Review Punkt 2)
segeval==2.0.11
jupyterlab>=4.0
notebook>=7.0
nbformat>=5.9
nbconvert>=7.0
ipykernel>=6.0
streamlit>=1.30
